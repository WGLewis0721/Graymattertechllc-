# Source / build notes

All assets in this package were built as original SVG production artwork from `build_media_package.py`. Editable SVG source is intentionally retained as the production source. Rasters are derivative exports for compatibility.
